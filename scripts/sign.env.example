# 이 파일을 scripts/sign.env 로 복사한 뒤 본인 값으로 채우세요.
# scripts/sign.env 는 .gitignore에 등록되어 깃에 올라가지 않습니다.
#
# `security find-identity -v -p codesigning` 출력의 따옴표 안 문자열을 그대로 사용
SIGN_ID="Developer ID Application: 본인이름 (TEAMID)"

# `xcrun notarytool store-credentials` 로 등록한 프로파일 이름
NOTARY_PROFILE="AC_NOTARY"
